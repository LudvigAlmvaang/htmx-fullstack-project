{
  "name": "htmx-project",
  "version": "0.0.1 (Alpha)",
  "description": "HTMX Demo",
  "scripts": {
    "backend": "cd backend/src && node main.js",
    "frontend": "cd frontend && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ludvig S.W. Almvång",
  "license": "Apache-2.0",
  "dependencies": {
    "eslint": "^9.7.0"
  }
}