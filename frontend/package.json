{
  "name": "htmx-frontend",
  "version": "0.0.1 (Alpha)",
  "description": "HTMX + Bootstrap demo",
  "main": "/src/js/main.js",
  "scripts": {
    "start": "parcel serve src/index.html --public-url / --dist-dir dist --port 5000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ludvig S.W. Almvång",
  "license": "Apache-2.0",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "htmx.org": "^2.0.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0"
  }
}
